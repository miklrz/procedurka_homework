#include <iostream>
using namespace std;

int main() {
	int N, K;
	cout << "¬ведите N: "; if (!(cin >> N)) { cout << "¬ведите корректные данные!"; return 1; }
	cout << "¬ведите K: "; if (!(cin >> K)) { cout << "¬ведите корректные данные!"; return 1; }
	bool m[1000];
	m[0] = true;
	m[11] = true;
	int max_len = 10;
	for (int i = 0; i < K; ++i) {
		int mesto;
		if (max_len % 2 == 1) {
			mesto = max_len 
		}
	}
}

// 1 _ _ _ _ _ _ _ _ _ _ 1 